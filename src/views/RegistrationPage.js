import React from 'react';   
import {observer} from 'mobx-react';

const RegistrationPage = observer((props) => {
    return (
        <div>Registration Page</div>
    )
})

export default RegistrationPage;